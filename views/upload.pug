extends layout

block content
  h1= title
  p Welcome to #{title}
  form
   button(formaction="/homeConsole") BackToConsole
  form(action='/upload', enctype='multipart/form-data', method='post')
   input(type='file', name='file-to-upload')
   input(type='submit', value='Upload')
